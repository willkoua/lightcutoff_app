{
  "name": "njuka-rules-tests",
  "description": "Tests des règles de sécurité Firestore/Storage (émulateurs)",
  "private": true,
  "scripts": {
    "test": "mocha",
    "test:emulator": "cd .. && firebase emulators:exec --config firebase.rules-test.json --project demo-njuka \"npm --prefix rules_tests test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.13.0",
    "mocha": "^10.7.3"
  }
}
